---
description: Blog app — routes, Sanity, AEO/GEO, robots; see apps/blog/CLAUDE.md
globs: apps/blog/**/*
alwaysApply: false
---

# Blog app (`@pakfactory/blog`)

When editing **`apps/blog/`**, follow [`apps/blog/CLAUDE.md`](../../CLAUDE.md), repo [`AGENTS.md`](../../../../AGENTS.md), and [`docs/blog-3-jira-conventions.md`](../../../../docs/blog-3-jira-conventions.md).

**Quick rules:**

- **Root URLs:** home `/`, posts `/[slug]`; `NEXT_PUBLIC_SITE_URL` is the blog origin (no `/blog` path prefix); use `getSiteUrl()` for canonicals and JSON-LD.
- **Routes:** App Router `/` + `/[slug]`; Server Components; `revalidate = 60` by default.
- **Sanity:** queries only from `@pakfactory/sanity/queries`; `getSanityClient()` + `isSanityConfigured()`; no inline GROQ in pages.
- **Robots (PROD-1495):** posts always index; listings use `getListingRobotsFromSearchParams` — page ≥2 or filters → noindex, follow.
- **AEO/GEO:** `generateMetadata` (OG + Twitter) on posts; **`BlogPosting`** JSON-LD via **`@pakfactory/seo`** + `serializeJsonLd` — never inline schema objects.
- **Components (ADR-008):** kebab-case `.tsx`, file name === exported component; **prefix-first stem** (`post-*`, `filter-*`, `site-*`). Grouped by **archetype/layer** in `src/components/{archetype}/` — `blocks/` (page-builder blocks), `layout/` (site chrome), `views/` (multi-route templates), `modules/` (Sanity-data-driven blocks), `ui/` (app-local primitives). Imported via `@/components/{archetype}/{file}` — never under `app/_components/`. Single-route whole-page views are inlined in `page.tsx` (ADR-007). App-local `ui/` ≠ cross-app `@pakfactory/ui`.
- **Blog pages (ADR-009):** `blogPage` with `pageRole` (`home` singleton, `landing`, `static`). Resolver at `/{segment}`: category → CMS page → post. **Studio:** **Pages → Homepage** until `BLOG_STUDIO_LANDING_PAGES` adds landing/static lists inside Pages. See [`CLAUDE.md`](../../CLAUDE.md).
- **Page builder (shipped):** home `/` renders home singleton `pageBuilder` via `BlockRenderer` + [`registry.ts`](../../src/components/blocks/registry.ts). Landings use `pageBuilderLanding` allowlist. Seed: `pnpm seed:blog-dev`. **Add a block:** Studio schema → component → `registry.ts` → GROQ in `PAGE_BUILDER_BLOCKS_PROJECTION`.
- **Tailwind:** no new tokens in `globals.css`; use `@pakfactory/ui` patterns.
- **UI primitives:** prefer `Card`, `Button`, `Badge`, `Input` from `@pakfactory/ui` for marketing bands and CTAs — not raw bordered `div`s when a primitive fits (see `apps/blog/CLAUDE.md`).

**Skills:** `seo-content-writer`, `on-page-seo-auditor`, `geo-content-optimizer` (root `CLAUDE.md`).

**Ops:** [`apps/blog/memory.md`](../../memory.md) for Vercel/DNS.
