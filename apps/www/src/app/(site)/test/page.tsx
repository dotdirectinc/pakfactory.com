export default function NavbarPage() {
    return (
        <div className="px-4 py-8">
            <p className="text-muted-foreground text-sm">
                Site chrome is the shared SiteNav from the layout.
            </p>
        </div>
    );
}
