# Copy to apps/studio/.env.local — must match repo root .env.local project + dataset.
SANITY_STUDIO_PROJECT_ID=""
SANITY_STUDIO_DATASET="development"
SANITY_STUDIO_PREVIEW_URL="http://localhost:3000"
