# Copy to apps/studio/.env.local — must match repo root .env.local project + dataset.
SANITY_STUDIO_PROJECT_ID=""
SANITY_STUDIO_DATASET="development"
# Presentation preview origins — names must match sanity.config.ts.
SANITY_STUDIO_PREVIEW_URL_WWW="http://localhost:3000"
SANITY_STUDIO_PREVIEW_URL_BLOG="http://localhost:3003"
